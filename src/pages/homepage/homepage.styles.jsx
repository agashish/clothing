import styled from 'styled-components';

export const HomePageContainer = styled.div`
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 20px 80px;
    // border: ${({isGeneric}) => isGeneric ? '1px solid grey' : '3px dotted red'};
`